[project]
name = "mini-resume-management"
version = "0.1.0"
description = "A FastAPI-based REST API for managing candidate resumes with CRUD operations and filtering capabilities"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.129.0",
]
